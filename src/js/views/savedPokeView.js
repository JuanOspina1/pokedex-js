class SavedPokeView {
  _parentElement = document.querySelector(".poke-list");

  // This generate markup will fill in the saved poke list
  _generateMarkup() {
    //

    return ``;
  }
}

export default new SavedPokeView();
